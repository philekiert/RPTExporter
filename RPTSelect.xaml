<bridgeopsclient:CustomWindow  xmlns:bridgeopsclient="clr-namespace:BridgeOpsClient" 
                               x:Class="RPTExporter.RPTSelect"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:cr="clr-namespace:SAPBusinessObjects.WPF.Viewer;assembly=SAPBusinessObjects.WPF.Viewer"
        Title="RPT Selector" Height="600" Width="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Button x:Name="btnDirectory" Content="Directory" Margin="10 15 10 5" Width="80" Click="btnDirectory_Click"/>
        <Button x:Name="btnExport" Content="Export" Margin="10 15 10 5" Grid.Column="1" Width="80"
                Style="{StaticResource primary-button}" HorizontalAlignment="Left"
                Click="btnExport_Click"/>
        <Label x:Name="lblDirectory" FontStyle="Italic" Grid.Row="1" Margin="6 0 10 10" Grid.ColumnSpan="2"/>
        <ListView x:Name="lst" Grid.Row="2" Margin="10 0 10 10" Grid.ColumnSpan="2"
                  ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollBarVisibility="Disabled"
                  Padding="4 4" MouseDoubleClick="lst_MouseDoubleClick" SelectionMode="Single">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Vertical" />
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Width="Auto" Margin="5 1">
                        <TextBlock Text="{Binding NameWithoutExtension}" TextAlignment="Center" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</bridgeopsclient:CustomWindow>